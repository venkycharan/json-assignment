# json-assignment

This repository contains a simple Python script demonstrating JSON handling tasks. The main script is named `json_assignment.py` (was previously `json.py`, which caused a naming conflict with Python's standard library).

## Usage

```bash
python3 json_assignment.py
```
